@extends('admin.content.layout')
@section('title','Concours')
@section('content')
@php($statuses=['draft'=>'Brouillon','registration_open'=>'Inscriptions ouvertes','registration_closed'=>'Inscriptions clôturées','completed'=>'Concours terminé','results_preparation'=>'Résultats en préparation','results_published'=>'Résultats publiés','archived'=>'Archivé'])
<div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-4"><div><h1 class="h2 mb-1">Concours</h1><p class="text-muted mb-0">Gérez tout le cycle des concours de l’INSG.</p></div><a class="btn btn-primary" href="{{ route('admin.contests.create') }}"><i class="bi bi-plus-lg me-2"></i>Nouveau concours</a></div>
<form class="cms-card p-3 mb-4"><div class="row g-3"><div class="col-md-7"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Rechercher par titre ou référence"></div><div class="col-md-3"><select class="form-select" name="status"><option value="">Tous les statuts</option>@foreach($statuses as $key=>$label)<option value="{{ $key }}" @selected(request('status')===$key)>{{ $label }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-primary w-100">Rechercher</button></div></div></form>
<div class="cms-card p-3 p-lg-4"><div class="table-responsive"><table class="table cms-table table-hover align-middle"><thead><tr><th>Concours</th><th>Session</th><th>Date</th><th>Candidats</th><th>Statut</th><th></th></tr></thead><tbody>@forelse($contests as $contest)<tr><td><strong>{{ $contest->title }}</strong><small class="d-block text-muted">{{ $contest->reference }}</small></td><td>{{ $contest->session }}<small class="d-block text-muted">{{ $contest->academic_year }}</small></td><td>{{ $contest->exam_date->format('d/m/Y') }}</td><td>{{ $contest->applications_count }} / {{ $contest->available_places }} places</td><td><span class="badge cms-status {{ $contest->status==='results_published'?'approved':($contest->status==='draft'?'pending':'') }}">{{ $statuses[$contest->status] }}</span></td><td class="text-end"><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.contests.show',$contest) }}">Gérer</a></td></tr>@empty<tr><td colspan="6" class="text-center py-5 text-muted">Aucun concours créé.</td></tr>@endforelse</tbody></table></div>{{ $contests->links() }}</div>
@endsection
