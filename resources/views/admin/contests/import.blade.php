@extends('admin.content.layout')
@section('title','Aperçu de l’import')
@section('content')<div class="d-flex align-items-center gap-3 mb-4"><a class="btn btn-outline-secondary" href="{{ route('admin.contests.results',$contest) }}"><i class="bi bi-arrow-left"></i></a><div><span class="cms-topbar-kicker">Import sécurisé</span><h1 class="h2 mb-0">Aperçu des résultats</h1></div></div>@if($errors)<div class="alert alert-danger"><strong>Le fichier contient des erreurs et ne peut pas être confirmé.</strong><ul class="mb-0 mt-2">@foreach($errors as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif<div class="cms-card p-4"><p><strong>{{ count($rows) }}</strong> ligne(s) valide(s) détectée(s).</p><div class="table-responsive"><table class="table cms-table"><thead><tr><th>ID candidature</th><th>Moyenne</th></tr></thead><tbody>@foreach($rows as $id=>$average)<tr><td>{{ $id }}</td><td>{{ number_format($average,2,',',' ') }}</td></tr>@endforeach</tbody></table></div>@if(!$errors && $rows)<form method="POST" action="{{ route('admin.contests.import.confirm',$contest) }}" onsubmit="return confirm('Confirmer l’import de ces résultats ?')">@csrf<input type="hidden" name="token" value="{{ $token }}"><button class="btn btn-primary">Confirmer l’import</button></form>@endif</div>@endsection
