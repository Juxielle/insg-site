@extends('admin.content.layout')
@section('title','Candidatures — '.$contest->title)
@section('content')
@php($labels=['pending'=>'En attente','validated'=>'Validée','rejected'=>'Refusée','correction_required'=>'À corriger'])
<div class="d-flex flex-wrap align-items-center gap-3 mb-4"><a class="btn btn-outline-secondary" href="{{ route('admin.contests.show',$contest) }}"><i class="bi bi-arrow-left"></i></a><div class="flex-grow-1"><span class="cms-topbar-kicker">{{ $contest->reference }}</span><h1 class="h2 mb-0">Candidatures</h1></div>@if($contest->isRegistrationOpen())<a class="btn btn-primary" href="{{ route('admin.contests.applications.create',$contest) }}"><i class="bi bi-person-plus me-2"></i>Inscrire un candidat</a>@endif</div>
<form class="cms-card p-3 mb-4"><div class="row g-3"><div class="col-md-7"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Nom ou matricule"></div><div class="col-md-3"><select class="form-select" name="status"><option value="">Tous les statuts</option>@foreach($labels as $key=>$label)<option value="{{ $key }}" @selected(request('status')===$key)>{{ $label }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-primary w-100">Filtrer</button></div></div></form>
<div class="cms-card p-3 p-lg-4"><div class="table-responsive"><table class="table cms-table align-middle"><thead><tr><th>Candidat</th><th>Matricule</th><th>Inscription</th><th>Statut</th><th></th></tr></thead><tbody>@forelse($applications as $application)<tr><td><strong>{{ $application->candidate->last_name }} {{ $application->candidate->first_names }}</strong><small class="d-block text-muted">{{ $application->candidate->email }}</small></td><td>{{ $application->candidate->registration_number ?: 'Après validation' }}<small class="d-block text-muted">{{ $application->candidate_number }}</small></td><td>{{ $application->submitted_at->format('d/m/Y H:i') }}<small class="d-block text-muted">{{ $application->source==='public'?'Site public':'Administration' }}</small></td><td><span class="badge cms-status {{ $application->status==='validated'?'approved':($application->status==='rejected'?'rejected':'pending') }}">{{ $labels[$application->status] }}</span></td><td class="text-end"><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.contests.application',$application) }}">Examiner</a></td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-5">Aucune candidature.</td></tr>@endforelse</tbody></table></div>{{ $applications->links() }}</div>
@endsection
